import React, {useState} from 'react';

const ShoppingCart = () => {

  const [cart, setCart]=useState(in)

  return (
    <div>
      <h1>Shopping Cart</h1>
    </div>
  );
}

export default ShoppingCart;
